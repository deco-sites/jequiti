import Header from "deco-sites/jequiti/components/header/Header.tsx";
import type { Props } from "deco-sites/jequiti/components/header/Header.tsx";

function HeaderSection(props: Props) {
  return <Header {...props} />;
}

export default HeaderSection;
