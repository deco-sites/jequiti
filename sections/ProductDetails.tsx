import ProductDetails, {
  Props,
} from "deco-sites/jequiti/components/product/ProductDetails.tsx";

function ProductDetailsSection(props: Props) {
  return <ProductDetails {...props} />;
}

export default ProductDetailsSection;
